package com.style;

import com.style.AsiaTShirt;

public class TShirt extends AsiaTShirt {
    // Every class that inherits from super class will attempts to insert super() at the default no args constructor if no own
    // constructor is created, for the super class doesn't have no args constructor will failed to compiled, and we will need
    // called the parameters constructor to made it compile
}
